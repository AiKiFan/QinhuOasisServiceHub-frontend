# 收藏功能修复指南

## 问题诊断

### 根本原因
- **问题**: 点击收藏按钮后显示"已收藏",但刷新"我的收藏"页面显示空列表
- **根本原因**: `toggleFavorite()` 函数中catch块为空,导致API调用失败时:
  1. 前端状态已更新为"已收藏"
  2. 但实际API请求可能失败(网络问题、认证问题等)
  3. catch块不恢复状态,用户看到错误的收藏状态
  4. 数据库中没有保存收藏记录

### 修复内容
已在3个详情页修复错误处理逻辑:
- [src/pages/restaurant/detail.vue](src/pages/restaurant/detail.vue)
- [src/pages/interpreter/detail.vue](src/pages/interpreter/detail.vue)  
- [src/pages/scenic/detail.vue](src/pages/scenic/detail.vue)

修复要点:
1. API调用失败时恢复到之前的状态
2. 添加console.log调试日志追踪API调用
3. 显示错误提示给用户

## 测试步骤

### 前端测试

1. **启动开发服务器**
   ```bash
   cd d:\me\ycu\qinhu-smart-tourism-cloud-platform\QinhuOasisServiceHub-frontend
   npm run dev:mp-weixin
   ```

2. **在微信开发者工具中测试**
   - 打开微信开发者工具
   - 导入项目: `dist/dev/mp-weixin` 目录
   - 登录测试账号
   - 进入餐厅详情页,点击收藏按钮
   - 打开控制台查看日志:
     ```
     [收藏] 切换前状态: false 餐厅ID: 1
     [收藏] 调用addFavorite API
     [收藏] addFavorite成功
     ```
   - 如果失败会显示:
     ```
     [收藏] API调用失败: Error: xxx
     ```

3. **验证"我的收藏"页面**
   - 进入"我的收藏"页面
   - 应显示刚才收藏的餐厅
   - 如果显示空列表,检查后端日志

### 后端诊断

1. **检查数据库**
   ```sql
   SELECT * FROM biz_user_favorite
   WHERE user_id = 2 AND deleted = 0
   ORDER BY create_time DESC;
   ```

2. **检查后端日志** (IDEA控制台)
   成功的SQL日志应该显示:
   ```
   ==>  Preparing: INSERT INTO biz_user_favorite...
   ==> Parameters: 2(Long), restaurant(String), 1(Long), null
   <==    Updates: 1
   ```

   查询时应该显示:
   ```
   ==>  Preparing: SELECT target_id FROM biz_user_favorite...
   ==>Parameters: 2(Long), restaurant(String)
   <==      Total: 1  (而不是0)
   ```

3. **如果数据库查询返回0**
   说明添加收藏API调用失败,检查:
   - Token是否正确传递(Authorization header)
   - 后端是否返回200状态码
   - 前端request.js是否正确处理响应

### API测试 (独立测试)

打开 [test-favorites.html](test-favorites.html) 进行API直接测试:

1. 点击"登录"按钮获取token
2. 点击"添加收藏"测试POST `/favorites`接口
3. 点击"查询收藏"测试GET `/favorites`接口
4. 点击"检查收藏状态"测试GET `/favorites/check/{type}/{id}`接口

查看浏览器控制台的完整响应。

## 可能的其他问题

### 如果收藏仍无法显示

检查以下几点:

1. **Token传递**
   - 前端: `src/utils/request.js` 第39行应添加Authorization header
   - 后端: `AuthInterceptor` 应正确解析token并设置LoginUser

2. **数据库表结构**
   确认表已创建:
   ```sql
   SHOW TABLES LIKE 'biz_user_favorite';
   DESC biz_user_favorite;
   ```

3. **Mapper配置**
   - 检查 `UserFavoriteMapper.xml` 的SQL语句
   - 确认 `deleted` 字段默认值为0

4. **前端API调用**
   确认使用正确的API:
   - ✅ 使用 `@/api/favorites` (后端API)
   - ❌ 不使用 `@/utils/favorites` (localStorage本地存储)

## 验证清单

- [ ] 点击收藏按钮后控制台显示成功日志
- [ ] 后端控制台显示INSERT SQL语句
- [ ] 数据库中有收藏记录(user_id=2, deleted=0)
- [ ] "我的收藏"页面显示收藏的餐厅
- [ ] 刷新页面后收藏状态保持
- [ ] 点击取消收藏后记录被删除(deleted=1)
- [ ] API失败时前端显示错误提示并恢复状态